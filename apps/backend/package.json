{
  "name": "backend",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "env:generate": "varlock codegen",
    "env:validate": "varlock load"
  },
  "dependencies": {
    "hono": "^4.13.0",
    "varlock": "^1.16.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
